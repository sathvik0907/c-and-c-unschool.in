#include <stdio.h>

int main()
{
    int a,b,sum,diff,mul,divi,per;
    printf("enter the values of a and b");
    scanf("%d%d",&a,&b);
    sum=a+b;
    diff=a-b;
    mul=a*b;
    divi=a/b;
    per=a%b;
    printf("sum of %d and %d is %d",a,b,sum);
    printf("difference of %d and %d is %d",a,b,diff);
    printf("product of %d and %d is %d",a,b,mul);
    printf("divison of  %d and %d is %d",a,b,divi);
    printf("per of %d and %d is %d",a,b,per);

    return 0;
}
